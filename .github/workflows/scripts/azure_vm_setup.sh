#!/bin/bash

# Custom Azure VM setup commands

# Example 1: Update the package list and upgrade installed packages
sudo apt update
sudo apt upgrade -y

# Example 2: Install additional software packages
# sudo apt install -y <package_name>

# Example 3: Configure your application or environment
# Some setup or configuration specific to your project
